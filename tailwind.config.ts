/** @type {import('tailwindcss').Config} */
module.exports = {
  theme: {
    extend: {
      fontFamily: {
        funnel: ['var(--font-funnel-display)', 'sans-serif'],
      },
    },
  },
  // ... rest of config
};